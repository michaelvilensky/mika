# macOS junk
.DS_Store
**/.DS_Store

# Claude Code local-only settings (machine-specific permissions/paths)
.claude/settings.local.json

# Editor noise
.vscode/
.idea/
*.swp
*~

# Node (in case we add a build step later)
node_modules/
package-lock.json
*.log

# Python bytecode from the voice-pack build script
__pycache__/
*.pyc

# Voice-pack build output — regenerate with:
#   cd alon-missions/voice && python3 make_audition.py
#   cd alon-missions/voice && python3 variants.py build
alon-missions/voice/audition.html
alon-missions/voice/variants/
